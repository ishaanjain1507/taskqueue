version: "2"

run:
  tests: true

linters:
  default: none
  enable:
    - govet
    - errcheck
    - staticcheck
    - ineffassign
    - gosimple
    - gocyclo
    - goconst
    - gofmt
    - goimports
  settings:
    govet:
      enable-all: true
    gocyclo:
      min-complexity: 15
    goconst:
      min-len: 3
      min-occurrences: 3
